<br clear="all">
<h3 class="text-center">Select a Service Plan</h3><br clear="all">
<div class="row justify-content-center">
  {{#each model.servicePlans as |plan|}}
    <div class="col-4 px-2 py-2">
      <div class="card">
        <div class="card-header text-center">
          <h5 class="card-title">{{plan.name}}</h5>
        </div>
        <div class="card-body">
        </div>
      </div>
    </div>
  {{/each}}
</div>
{{outlet}}